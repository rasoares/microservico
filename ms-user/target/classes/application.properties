spring.application.name=ms-user

#Configura��o para porta aleatoria
server.port=${POT:0}
eureka.instance.instance-id=${spring.application.name}:${spring.apllication.instance_id:${random.value}}

#Configura��o para o registro no Eureca Server
eureka.client.server-url.defaultZone= http://localhost:8761/eureka
